foods = ['duck','cats','dingleberries']

for f in foods:
    print(f)
    if f is 'duck':
        print("Ducks are delicious...mmmmm.")

#always put a colon after a condition and remember to tab over if it doesn't automatically do it

#whenever you want to print out the length of something, the function is len
    print(f)
    print(len(f))

